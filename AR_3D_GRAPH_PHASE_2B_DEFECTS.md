# AR 3D Graph Phase 2B — Defect Register

| ID | Area / class | Severity | Emulator | Steps | Expected | Actual | Cause | Fix | Retest |
| --- | --- | ---: | --- | --- | --- | --- | --- | --- | --- |
| P2B-001 | Generation lifecycle / Phase 2 introduced | Medium | JVM + API 35 | Start queued generation, leave AR screen, allow task to finish | Work cancels; stale result cannot publish | Activity-owned ViewModel could outlive the composable and publish later | No explicit screen-exit cancellation/invalidation | Added `onScreenExit()`, Future cancellation, generation-ID invalidation and stable state recovery; dispose hook calls it | Specific cancellation tests, 42/42 AR JVM, 2/2 connected, both cycles pass |
| P2B-002 | Renderer/anchor cleanup / Phase 2 introduced | High | JVM + API 35 | Pause camera view, then dispose/exit | Anchor and CPU resources release immediately | Cleanup was only queued to the GL thread, which might not run again after pause | Disposal depended on a future GL callback | Dispose now closes renderer CPU/anchor state immediately and retains queued GL-object deletion | Renderer/anchor/lifecycle groups, 20 reopen cycles, exit/logcat checks pass |
| PRE-001 | Existing connected UI assertions / Pre-existing | High as release gate, not an AR defect | API 35 | Run `CrossWorkspaceIntegrationUiTest` | Assertions accommodate persisted/duplicate semantic nodes | 3/5 fail on stale `Equations (5)` or exactly-one surface assumptions | Existing test/product state predates Phase 2B; test file already user-modified | Not changed per scope restrictions | Reproduced; 2/5 pass, 3/5 remain |
| PRE-002 | Broad connected suite / Pre-existing | High as release gate | API 35 | Run retained full connected suite | Green app-wide baseline | 34/61 finalized failures; 10/1,200 Solver matrix rows fail; ADB/UTP teardown error | Multiple stale existing expectations plus harness teardown instability | Not changed | Documented from retained run |
| ENV-001 | Live ARCore validation / Emulator limitation + physical-device validation required | Critical coverage gap | API 35 | Query package/start AR flow | ARCore session and camera tracking available | `com.google.ar.core` absent | Available AVD is not ARCore-capable | No fake/install workaround applied | Unsupported flow passes; live AR cases blocked |
| HAR-001 | Gradle filtering / Test harness | Low | Host | Pass an unquoted `-P...class` property through PowerShell | Filtered task launches | Gradle interpreted property as a task | Shell argument parsing | Quoted the Gradle property | Corrected command ran 2/2 tests |

No Critical mathematical, engine-corruption, crash or leak defect was found. Both introduced defects are fixed. Remaining failures are explicitly out of Phase 2B product-fix scope or environmental.
