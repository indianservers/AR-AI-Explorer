import fs from "node:fs/promises";import path from "node:path";
const root="D:/Math Images",tmp="C:/Indian Servers/AIExplorer/.codex_tmp_lesson_audit";const lessons=JSON.parse(await fs.readFile(path.join(tmp,"lessons_591_690.json"),"utf8")).slice(0,71);const ids=new Set(lessons.map(x=>x.lessonId));let changed=0;
async function walk(d){for(const e of await fs.readdir(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())await walk(p);else if(e.name==="metadata.json"){const j=JSON.parse((await fs.readFile(p,"utf8")).replace(/^\uFEFF/,""));let c=false;for(const x of j.images||[])if(ids.has(x.lessonId)){x.qaStatus="approved";c=true;changed++}if(c)await fs.writeFile(p,JSON.stringify(j,null,2),"utf8")}}}await walk(root);console.log(JSON.stringify({approved:changed}));
